#include "pch.h"
#include "AesTool.h"

 char* encryptAndBase64(const unsigned char* plain, const unsigned char* password)
{
	return nullptr;
}

 char* uncode64AndDecrypt(const unsigned char* plain, const unsigned char* password) {
	 return nullptr;
}
